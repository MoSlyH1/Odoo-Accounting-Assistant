{
  "name": "odoo-bill-agent",
  "version": "1.0.0",
  "description": "Scan vendor bills (Arabic/English) with free AI and create draft bills in Odoo.",
  "type": "module",
  "main": "src/server.js",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node src/server.js",
    "dev": "node --watch src/server.js",
    "migrate": "node src/db/migrate.js"
  },
  "dependencies": {
    "express": "^4.21.2",
    "multer": "^1.4.5-lts.1",
    "pg": "^8.13.1"
  }
}
